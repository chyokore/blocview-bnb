import { NextResponse } from "next/server";
import { RANGE_PILOT_TASK_ORIGIN, sanitizeTask, taskError, validTaskId } from "@/lib/range-pilot-hiring";
export async function GET(_request: Request, { params }: { params: Promise<{ taskId: string }> }) { const taskId = (await params).taskId; if (!validTaskId(taskId)) return NextResponse.json(taskError(404), { status: 404 }); const token = process.env.BLOCVIEW_SERVICE_TOKEN; if (!token) return NextResponse.json(taskError(503), { status: 503 }); try { const response = await fetch(`${RANGE_PILOT_TASK_ORIGIN}/tasks/${encodeURIComponent(taskId)}`, { headers: { Accept: "application/json", Authorization: `Bearer ${token}` }, signal: AbortSignal.timeout(15_000), cache: "no-store" }); if (!response.ok) return NextResponse.json(taskError(response.status), { status: response.status === 404 ? 404 : 503 }); const task = sanitizeTask(await response.json()); return task ? NextResponse.json(task) : NextResponse.json(taskError(503), { status: 503 }); } catch { return NextResponse.json(taskError(503), { status: 503 }); } }
